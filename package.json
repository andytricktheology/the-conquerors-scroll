{
  "name": "the-conquerors-scroll",
  "version": "1.0.0",
  "description": "A biblical truth platform for Trick Theology",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "dev": "npx @11ty/eleventy --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "marked": "^5.0.2"
  }
}